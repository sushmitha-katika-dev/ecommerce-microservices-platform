# Postman Testing

A full Integration suite is available in the `postman/` directory at the repository root.

- Import `postman_collection.json` and `postman_environment.json`.
- Set environment to "E-Commerce Local Env".
- Run the "Login" request to automatically extract and inject the JWT token for subsequent requests.
